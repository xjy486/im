---
status: accepted
---

# 支持公开用户注册并由服务端分配账号

登录页同时提供注册入口。新用户提交昵称和至少 8 位密码后，服务端在事务中创建用户、从全局公开号码命名空间分配不可变的 11 位账号、创建首个设备和认证会话，并返回与登录相同的 Access/Refresh Token 响应。客户端不允许用户自选或修改账号。

公开注册按 IP 地址限速，服务端不记录密码、Token 或注册表单内容；成功注册记录不含内容的 `REGISTER` 安全审计事件。账号仍只能使用完整 11 位数字登录和精确搜索。
