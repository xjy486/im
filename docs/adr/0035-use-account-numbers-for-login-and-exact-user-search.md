---
status: accepted
---

# 使用账号登录并进行精确用户搜索

预注册用户以 `account_no + password` 登录，昵称仅用于展示。用户搜索只接受完整账号精确匹配，不支持昵称、账号前缀或模糊搜索，结果只包含账号、昵称、头像及联系人申请状态；用户可以关闭 `searchable_by_account_no`。账号和搜索接口按用户及 IP 限速，并用统一未找到响应隐藏不可见实体。
