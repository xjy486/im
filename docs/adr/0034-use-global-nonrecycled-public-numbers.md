---
status: accepted
---

# 使用全局唯一且永不复用的公开数字号码

用户账号和群号共用 `public_identifiers` 全局命名空间，对外号码为首位非零的十位安全随机数字加一位 Luhn 校验位，并以 `CHAR(11)` 保存。内部主键和所有外键仍使用 UUIDv7，公开号码只用于展示、登录和搜索。号码不可修改或自选，账号注销或群解散后只写入 `retired_at`，永不重新分配；数据库唯一约束处理并发碰撞并重新生成。
