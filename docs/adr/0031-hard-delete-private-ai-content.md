---
status: accepted
---

# 用户删除和账号注销时硬删除私人 AI 内容

账号注销开始时自动取消该用户所有 `QUEUED` 或 `RUNNING` AI 任务并阻止其写回结果，不要求用户等待任务结束。

用户删除总结、回复建议或待办时硬删除内容，并通过私人同步事件让其 MOBILE 与 PC 删除本地副本；任务可以保留最多 30 天的不含内容状态和错误码。退出登录保留私人 AI 数据，账号注销则级联删除 AI 任务、artifact、action item、私人 AI 同步事件和缓存，只允许保留无法关联回用户的聚合用量统计。
